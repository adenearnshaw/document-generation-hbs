<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <title>Home Page</title>
    <style>
        body {
            background-color: #f3f3f3;
            color: #212121;
        }
    </style>
</head>

<body>
    <header>
        {{#with Header}}
        <h1>{{title}}</h1>
        {{#if subtitle}}
        <h2>{{subtitle}}</h2>
        {{else}}
        <br />
        {{/if}}

        {{/with}}
    </header>

    <main>
        <div>
            <p>{{description}}</p>

            <div>
                <p>The key benefits are:</p>
                <ul>
                    {{#each benefits}}
                    <li>{{.}}</li>
                    {{/each}}
                </ul>
            </div>
        </div>
    </main>
</body>

</html>